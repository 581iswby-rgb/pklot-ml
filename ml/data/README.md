# UrbanEV 训练数据

`urbanev_hourly.csv` 是 UrbanEV 的 `volume-11kW.csv` 与 `inf.csv` 转换结果：

- 275 个区域虚拟站点；
- 4,344 个连续小时；
- 1,194,600 条记录；
- 字段符合 `pklot_ml` 的 `timestamp_epoch,station_id,total_piles,load_kw` 契约。

原始数据与许可见 https://github.com/IntelligentSystemsLab/UrbanEV 。
